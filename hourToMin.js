function hourToMin(hour){
  let Min = 60;
  let converHour;
  converHour = hour * Min;
  return converHour;
  }
  let inputHour = 2;
  let outputHour = hourToMin(inputHour);
  console.log(outputHour);